@page "/cars"
@inject ICarService CarService

<h3>Cars</h3>

@if (cars.Any())
{
    <span>Loading...</span>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Model</th>
                <th>Year</th>
                <th>Mileage</th>
                <th>BodyStyle</th>
                <th>Color</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var car in cars)
            {
                <tr>
                    <td>@String.Format($"{car.Make} {car.Model}")</td>
                    <td>@car.Year</td>
                    <td>@car.Mileage</td>
                    <td>@car.BodyStyle</td>
                    <td>@car.Color</td>
                </tr>
            }
            
        </tbody>
    </table>
}


@code {
    IEnumerable<CarDTO> cars = new List<CarDTO>();
    protected override async Task OnInitializedAsync()
    {
        cars = await CarService.GetCars();
    }
}
